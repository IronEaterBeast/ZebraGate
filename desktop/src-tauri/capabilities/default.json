{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the ZebraGate desktop windows",
  "windows": ["main", "group-management", "error-log"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "opener:default",
    "dialog:default"
  ]
}
